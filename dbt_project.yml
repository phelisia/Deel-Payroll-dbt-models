name: 'deel_payroll_analytics'
version: '1.0.0'
config-version: 2

profile: 'deel_payroll_analytics'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  deel_payroll_analytics:
    staging:
      +schema: raw_staging
    intermediate:
      +schema: raw_intermediate
    core:
      mart:
        +schema: raw_analytics
      exposure:
        +schema: raw_analytics
        +materialized: view
